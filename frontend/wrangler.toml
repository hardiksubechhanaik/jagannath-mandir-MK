name = "shreemandirmk"
compatibility_date = "2025-06-01"

# Cloudflare Workers Builds still uses the legacy frontend/ root directory.
[build]
command = "npm install --prefix .. && npm run build --prefix .."

[assets]
directory = "../dist"
not_found_handling = "single-page-application"
